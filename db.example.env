# Copy to db.env and set a real password. db.env is git-ignored and is read by
# both the postgres container (via podman-compose env_file) and Django settings.
POSTGRES_DB=typelearn
POSTGRES_USER=typelearn
POSTGRES_PASSWORD=change-me
