# The API is same-origin: the Gateway routes /graphql/ to Django and everything
# else here, so a relative path is all the frontend needs and there is no
# cross-origin request to configure. This is why the backend carries no CORS
# settings at all.
VITE_API_URL=/graphql/
